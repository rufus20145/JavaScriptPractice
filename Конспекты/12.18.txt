События, обработчики событий, слушатели
    Событие - что-то, что может произойти во внешней среде
    Событие порождает реакцию
    Список событий обычно задан заранее

    слушатели - отдельный объект, отлавливающий события
        можно передать несколько объектов для отслеживания событий
            или несколько событий на один объект
            или несколько функций на одно событие

Работа с обработчиками событий и слушателями в JS



Погружение и всплытие
    event.target - элемент, с которым непосредственно произошло взаимодействие

Событийно-ориентированное программирование
    event-driven
    создание реакций программы на действия пользователя

Библиотеки и фреймворки
    JQuery - библиотека (елочная игрушка, которую мы вешаем на ёлку - на наш код)
    ReactJS - фреймворк (сама ёлка, на которую мы вешаем игрушки - наш код)

Библиотеки и фреймворки JS
    Библиотеки
        JQuery
        JQuery UI
        Underscore
        Backbone
    Фреймворки
        ReactJS
        AngularJS
        VueJS
        ExtJS (2000$)
        RevealJS
    Среда Node.JS
        